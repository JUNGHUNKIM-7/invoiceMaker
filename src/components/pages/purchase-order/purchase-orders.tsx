import type { NextPage } from 'next'
import PageController from '../page-controller'

const PurchaseOrders: NextPage = () => {
    return <PageController title={'purchase order'} />
}

export default PurchaseOrders
