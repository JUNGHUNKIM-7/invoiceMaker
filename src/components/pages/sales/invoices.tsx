import type { NextPage } from 'next'
import PageController from '../pageController'

const Invoices: NextPage = () => {
    return <PageController title={'invoicePages'} />
}

export default Invoices
